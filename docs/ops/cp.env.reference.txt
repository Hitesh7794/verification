# =========================================================
#  Control Plane (CP) — /opt/verificationportal/cp.env
#  Reference file. Copy to the CP box and fill in values
#  marked <...>. All secrets rotate per SOP-VP-001.
#  Current live box: 13.205.220.104
# =========================================================


# ── HTTP server ───────────────────────────────────────────
CP_HTTP_ADDR=:8091
APP_ENV=production
ALLOWED_ORIGINS=https://admins.13-205-220-104.nip.io,https://verifyportal.13-205-220-104.nip.io
PUBLIC_BASE_URL=https://admins.13-205-220-104.nip.io


# ── Database (RDS Postgres) ───────────────────────────────
# Separate DB from the DPs — CP owns clients_registry +
# institution_applications; each DP owns its own schema.
CP_DATABASE_URL=postgres://<user>:<pass>@<rds-host>:5432/verification_cp?sslmode=require


# ── Auth ──────────────────────────────────────────────────
# Signs CP superadmin JWTs. Independent from DP JWT_SECRET
# so a CP token doesn't work on a DP and vice versa.
CP_JWT_SECRET=<64 hex chars — openssl rand -hex 32>


# ── Federated fetch to Data Planes ────────────────────────
# Per-DP timeout when CP calls each DP's /api/internal/metrics
# to build the aggregated dashboard. Also used for internal
# fan-out calls (org provisioning, exam list, reviewer list).
FEDERATED_TIMEOUT_MS=3000


# ── Notes ─────────────────────────────────────────────────
# CP does NOT hold any per-client INTERNAL_API_KEY in its env.
# It stores them in the DB (clients_registry.api_key) and looks
# them up per request. Rotation = one UPDATE on CP + one .env
# edit on the target DP.
