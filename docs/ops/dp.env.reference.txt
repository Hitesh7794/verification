# =========================================================
#  Data Plane (DP) — /opt/verificationportal/.env
#  Reference file. Copy to the DP box and fill in values
#  marked <...>. All secrets rotate per SOP-VP-001.
#  Current live box: 13.127.17.248 (NTA)
# =========================================================


# ── HTTP server ───────────────────────────────────────────
HTTP_ADDR=:8080
APP_ENV=production
ALLOWED_ORIGINS=https://verifyportal.13-127-17-248.nip.io,https://nta.13-127-17-248.nip.io
PUBLIC_BASE_URL=https://verifyportal.13-127-17-248.nip.io


# ── Database (RDS Postgres) ───────────────────────────────
DATABASE_URL=postgres://<user>:<pass>@<rds-host>:5432/verification?sslmode=require


# ── Auth ──────────────────────────────────────────────────
# Signs every DP user JWT (admin, reviewer, agent). Rotate = every
# user logged out; coordinate a maintenance window.
JWT_SECRET=<64 hex chars — openssl rand -hex 32>


# ── Storage ───────────────────────────────────────────────
DATA_DIR=/opt/verificationportal/data
ARTIFACT_DIR=/opt/verificationportal/artifacts
DOWNLOADS_DIR=/opt/verificationportal/downloads
S3_BUCKET=trustview-verification-portal
S3_REGION=ap-south-1
PHOTOS_BACKEND=s3          # "s3" (prod) or "disk" (dev fallback)


# ── Biometric matchers ────────────────────────────────────
TRUSTVIEW_BASE_URL=https://v1.trustview.in
TRUSTVIEW_TOKEN=<vendor issued — rotate per vendor advisory>
LUXAND_BASE_URL=http://127.0.0.1:8041
LIVENESS_MAX_AGE_SECONDS=90
IRIS_MATCH_THRESHOLD=0.6


# ── Wallet + Razorpay ─────────────────────────────────────
# KEY_ID is shipped to the browser to init the payment widget.
# KEY_SECRET + WEBHOOK_SECRET stay server-side and verify signatures.
RAZORPAY_KEY_ID=rzp_live_<id>
RAZORPAY_KEY_SECRET=<private>
RAZORPAY_WEBHOOK_SECRET=<private>
WALLET_FEE_PER_LOOKUP_PAISE=500           # Rs.5 per candidate lookup
WALLET_MAX_DEPOSIT_PAISE=5000000          # Rs.50,000 cap per top-up
WALLET_SAME_ROLL_CACHE_MIN=1440           # don't re-charge same roll for 24h


# ── Email (SMTP) ──────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=inno.verifyportal@gmail.com
SMTP_PASS=<Gmail App Password — NOT the account password>
SMTP_FROM=Verification Portal <inno.verifyportal@gmail.com>


# ── SMS OTP (AuthKey.io) ──────────────────────────────────
AUTHKEY_SMS_KEY=<vendor issued>
AUTHKEY_SMS_SID=44529
AUTHKEY_SMS_COMPANY=seQRview
AUTHKEY_SMS_COUNTRY_CODE=91
AUTHKEY_SMS_URL=https://api.authkey.io/request


# ── Multi-tenant bridge to Control Plane ──────────────────
# Both INTERNAL_API_KEY and DATA_PLANE_API_KEY hold the SAME value
# for this DP. They're named differently to make the direction of
# each call obvious in headers.
#   CP -> DP direction: header  X-Internal-API-Key: <INTERNAL_API_KEY>
#   DP -> CP direction: header  X-Data-Plane-Api-Key: <DATA_PLANE_API_KEY>
#                        header  X-Data-Plane-Client-ID: <DATA_PLANE_CLIENT_ID>
INTERNAL_API_KEY=<same as clients_registry.api_key on CP for this client>
DATA_PLANE_API_KEY=<same value>
DATA_PLANE_CLIENT_ID=9                    # CP's clients_registry.id for this DP
CONTROL_PLANE_URL=http://13.205.220.104:8091
CONTROL_PLANE_ALLOWED_IPS=                # optional CIDR allowlist, e.g. 13.205.220.104/32
